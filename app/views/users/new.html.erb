<%= provide :title, t(".sign_up") %>
<%= provide :button_text, t(".sign_up") %>
<div class="register">
    <div class="container">
      <%= render "shared/error_messages" %>
      <div class="register-top heading">
        <h2><%= t ".register" %></h2>
      </div>
      <%= render "users/form" %>
    </div>
  </div>
